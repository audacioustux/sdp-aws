name: sdp-aws
description: Software Delivery Platform - AWS Infrastructure
config:
  pulumi:disable-default-providers:
    - kubernetes
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
runtime:
  name: nodejs
  options:
    nodeargs: '--import ./register.js'
